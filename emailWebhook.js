// placeholder: parse incoming email webhook and forward to /queries
module.exports = (req, res) => {
  // Parse email payload and call backend /queries to create a new Query
  res.sendStatus(200);
};
